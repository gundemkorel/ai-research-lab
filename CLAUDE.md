# Claude Code Instructions

Read and follow `AGENTS.md` as the repository's scientific constitution and `program.md` as the autonomous research procedure.

When working inside `experiments/`, also follow nested instruction files, especially each experiment's `eval/AGENTS.override.md`.

Do not replace these rules with generic coding-agent defaults. This repository is an evidence-driven research lab with bounded autonomy and explicit compute-cost controls.
