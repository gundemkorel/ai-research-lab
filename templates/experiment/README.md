# {{TITLE}}

Experiment id: `{{EXPERIMENT_ID}}`

This directory is the self-contained workspace for one research idea.

- `hypothesis.md` defines the question and decision rule.
- `config.yaml` contains experiment defaults.
- `src/` contains implementation/training code.
- `eval/` contains the evaluation protocol and is protected by stricter agent instructions.
- `runs/` contains immutable run records.
- `analysis/` contains analysis code and figures.
- `journal.md` records chronological reasoning from evidence.
- `report.md` contains the final concise result.
